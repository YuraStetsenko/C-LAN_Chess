
updatePieceMoves() updates all the pieces' sets of both availableMoves and attackedSquares.
To look for the checks we actually only need to update the attacked squares, so let's make a separate function to use in the isMoveValid(), so it doesn't mess up availableMoves sets.

After all decided to just rename availableMoves into potentialMoves for clarity. 
Temporarily leaving illegalMoves to be, because although I don't like it, it works with it.
Added legalMoves, and I'm going to replace the role of illegalMoves with it, because it would be more of a straightworward approach. 
TODO:: complete writing updateLegalMoves(), implement it, and replace the illegal moves approach bs
//FIXED:://Also, isMoveVaild() seems to work incorrectly when an EnPassant move is passed there. Let me check.
TODO:: look for for-if-if, else nests where I didn't use curly brackets. Might be some errors there lol...

Gonna fix selected piece moves displaying, then gonna split finding potentialMoves and attackedSquares into separate functions

OKAY. SOMETHING IS NOT RIGHT WITH THE SPLIT APPROACH.
Another mystery lol.

IDEA:: rework attacked cells as a 2d matrix mapping which cells are attacked by who with 2 bool flags for each cell

to do lots of debugging and to consider changing sets to something more reliable, as they cause some shit